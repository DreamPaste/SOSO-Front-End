// File: docs/tsconfig.json
{
  "extends": "astro/tsconfigs/strict",
  "include": [
    ".astro/types.d.ts",
    "src/**/*", // ← 여기서 src 경로를 명확히
    "*.config.mjs", // 설정 파일도 타입 체크 대상에 넣거나
    "*.config.js"
  ],
  "exclude": ["dist", "node_modules"]
}
